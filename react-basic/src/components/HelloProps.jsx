const HelloProps = ({name,color}) =>{
    // props.name
    return(
        <div className="helloProps">
            <div>HelloProps.jsx area props = {name} / props = {color}</div>
        </div>
    )
}

export default HelloProps;

